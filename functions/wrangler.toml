# Edge Gateway Dashboard — wrangler.toml (reference only)
# The active runtime is server.js, not wrangler. This file is kept
# for documentation of the original Cloudflare Worker configuration.

name = "edge-gateway-dashboard"
compatibility_date = "2025-06-01"
compatibility_flags = ["nodejs_compat"]

[vars]
BASE_DOMAIN = ""
PROXY_TARGET = ""
API_KEY = ""
ALLOWED_ORIGINS = ""
